name: 编译 OpenWrt x86_64 Full

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 下载仓库源码
      uses: actions/checkout@v4

    - name: 🕒 设置编译日期
      id: build_date
      run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

    - name: 🧹 释放磁盘空间
      run: |
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/lib/jvm
        sudo docker system prune -af || true
        sudo apt-get clean

    - name: 💾 增加 Swap（防止 Rust 等内存不足）
      run: |
        sudo fallocate -l 4G /swapfile
        sudo chmod 600 /swapfile
        sudo mkswap /swapfile
        sudo swapon /swapfile

    - name: 📦 移动编译目录到 /mnt
      run: |
        sudo mkdir -p /mnt/openwrt
        sudo chown $USER:$USER /mnt/openwrt
        cp -rf ./* /mnt/openwrt/

    - name: 🛠️ 安装编译依赖
      run: |
        sudo apt update
        sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
        gettext git libncurses5-dev libssl-dev python3 python3-venv python3-pip \
        rsync unzip zlib1g-dev file curl

    - name: 🌐 克隆官方 OpenWrt 源码
      working-directory: /mnt/openwrt
      run: |
        git clone https://github.com/openwrt/openwrt.git src
        cd src
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: ⚙️ 应用 config.seed
      working-directory: /mnt/openwrt
      run: |
        if [ -f config.seed ]; then
            cp config.seed src/.config
        else
            echo "⚠️ 未找到 config.seed，继续使用默认配置"
        fi

    - name: 🔧 执行 diy.sh 自定义脚本
      working-directory: /mnt/openwrt
      run: |
        if [ -f diy.sh ]; then
            chmod +x diy.sh
            ./diy.sh || true
        else
            echo "⚠️ 未找到 diy.sh，跳过"
        fi

    - name: 🏷️ 注入编译日期进版本号
      working-directory: /mnt/openwrt/src
      run: |
        DATE=${{ steps.build_date.outputs.date }}
        echo ">>> OpenWrt Version: $DATE"
        sed -i "s/OpenWrt/OpenWrt-$DATE/g" include/version.mk
        echo "$DATE" > package/base-files/files/etc/openwrt_build_date

    - name: 📦 下载依赖包
      working-directory: /mnt/openwrt/src
      run: |
        make defconfig
        make download -j8

    - name: 🏗️ 编译固件
      working-directory: /mnt/openwrt/src
      run: |
        make -j$(nproc) || make -j1 V=s

    - name: 📁 整理输出固件
      working-directory: /mnt/openwrt
      run: |
        mkdir -p output
        cp -rf src/bin/targets/*/*/* output/ || true

    - name: 📤 上传固件 Artifact
      uses: actions/upload-artifact@v4
      with:
        name: OpenWrt-x86_64-Full-${{ steps.build_date.outputs.date }}
        path: /mnt/openwrt/output
